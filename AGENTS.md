# AI開発フロー共通規約

このプロジェクトでは、役割別Skill（Codex: $pm, $tl, $implementer, $auditor / Claude: /auditor）による
AI開発フローを運用している。役割プロンプトが宣言された場合、その役割・ルールを最優先で守ること。
以下は全役割に共通する規約である。

## 成果物の置き場所

- フローの成果物はすべて `docs/flow/<機能名>/` 配下に置く（無ければ作成する）
- 工程ごとのファイル名:
  - `spec.md` 実装ドキュメント（正本） / `tasks.md` タスク分割
  - `task-NN/instruction.md` 指示書 / `pre-summary.md` 実装前サマリ
  - `task-NN/report.md` 完了報告 / `summary.md` 監査用仕様サマリ
  - `task-NN/report.md` 完了報告 / `summary.md` 監査用仕様サマリ
  - `task-NN/audit-request.md` 監査依頼 / `audit-codex.md` `audit-claude.md` 監査結果
  - `task-NN/audit-triage.md` 監査結果の整理
  - `tech-lead/<件名>.md` Tech Lead相談資料
- 成果物を書き出したら、そのパスを1行でユーザーへ報告する
- 他役割の成果物は、貼り付けを待たずファイルを直接読む
- 工程ファイル（`docs/flow/` 配下）はgitignore対象でコミットしない。規約ファイル（`AGENTS.md` / `CLAUDE.md`）も同様にgitignoreする。いずれもローカル作業用であり、リポジトリの履歴には残らない
- git履歴に工程記録が残らないため、機能クローズ後は spec.md の確定内容のうち恒久的に必要な事項を、正式ドキュメント（コミット対象のファイル）へ必ず反映する（反映は実装担当がPMの指示書に従って行う）
- 機能クローズ後も docs/flow/<機能名>/ は削除しない（ローカルに残す）

## 工程完了時のハンドオフ

成果物を書き出して工程が完了したら、ユーザーがフロー手順書を見なくても次へ進めるよう、以下の形式で報告する。

1. **要約**: 決まったこと・やったこと（3〜5行）
2. **書き出したファイル**: パス
3. **次のアクション**: 次に動かす独立セッション（PM / TL（Tech Lead） / 実装担当 / 基本2監査、オーナー指定時は1監査）。ユーザー自身が行うこと（承認、クローズ判定、git操作）があればそれを先に示す
4. **次のセッションへ貼るプロンプト**: そのままコピペできる文面をコードブロックで示す（読ませるファイルのパスを含める）

次のアクションがユーザー自身の判断・操作のみの場合は、プロンプトの代わりに「あなたがやること」を短く示す。監査への依頼は基本2セッション（Codex監査 / Claude監査）、オーナーが1監査を指定した場合はその1セッションへ貼る旨を添える。

## 役割セッションの独立性

- PM、TL（Tech Lead）、実装担当、Codex監査、Claude監査は、それぞれオーナーが開始する独立セッションとして扱う
- PMと実装担当は機能開発中の各自のセッションを継続利用してよい。TL（Tech Lead）は相談ごと、監査は監査ごとに独立セッションを開始する
- どの役割も、別の役割をサブエージェントとして起動・代行せず、サブエージェントの出力を正式な別役割の成果物として扱わない
- サブエージェントは、現在の役割と作業範囲の中での調査・分析・並列作業に限って利用してよい。親セッションが結果を確認し、その役割として責任を持つ
- 役割をまたぐときは、成果物と次セッション用プロンプトを提示して停止する。次の独立セッションを開始するのはオーナーだけとする
- 監査はCodexとClaudeの独立2監査を基本とし、オーナーがタスクごとに明示した場合だけ1監査にできる。PMや他役割が監査数を独自に減らしてはならない

## 表現ルール（人間が読む前提で書く）

成果物ファイルもユーザーへの報告も、フローを把握していない人間が読んで分かる日本語で書く。

- その場限りの略号・記号を作らない（例: `M1` `F1` `A-2`）。指摘や項目を指すときは「通し番号＋内容の要約」で書く（例: 「指摘3: ログイン失敗回数がリセットされない」）
- 他ファイルの項目を参照するときは、番号だけでなく要約とファイル名を添える（例: 「audit-codex.md の指摘3（ロック解除の時刻判定）」）
- 分類・重大度は日本語の言葉で書く（「今すぐ直すべき」「次タスクでよい」「残リスクとして受け入れる」）。記号・英字コード・独自の数値スケールに置き換えない
- 略語・専門用語は初出で1行の説明を添える（例: 「分岐網羅（C1。if/elseなどの各枝を最低1回通すこと）」）
- 「前述の関数」「例のファイル」で済ませず、ファイルパス・関数名・テスト名を実名で書く
- テンプレートで書式指定された定型タグ（`[PUBLIC API]` など）はそのまま使ってよいが、日本語の補足を添える。それ以外の英語の見出し・内部用語をそのまま貼らない

## git

- git変更操作（add / commit / branch作成 / checkout・switch / merge / push / PR作成 / force push / 履歴改変 / タグ操作）はすべてオーナー（ユーザー）が行う。AIセッションは役割を問わず実行しない
- git読み取り操作（status / log / diff / show / ブランチ確認）は各役割が自由に行ってよい
- 作業は指示されたfeatureブランチ上で行う。作業開始前に現在のブランチが指示と一致するか確認し、違う場合（ブランチ未作成を含む）は作業に入らず、ユーザーへ作成・切替を依頼する
- 作業単位が完了したら、変更ファイル一覧と推奨コミットメッセージをユーザーへ提示してコミットを依頼する。巨大な未コミット差分を滞留させない
- gitignore対象のファイル（`docs/flow/` 配下、`AGENTS.md` / `CLAUDE.md`）は変更ファイル一覧に含めない
- 監査範囲は `git diff <base>..HEAD` で確定する
- テスト・lint・型チェックはCIで必須化されている。CIと同等の検証をローカルで実行してから完了報告し、featureブランチのCIが通ることをタスク完了条件に含める

## 共通ルール

- 不明点は推測で埋めず、確認できないことは「不明」と明記する
- プロジェクト固有の技術構成・設計・規約は、既存ドキュメントと既存実装に従う
- 指示範囲外の変更・改善・リファクタ・削除を勝手に行わない
- 破壊的操作（削除、上書き、履歴改変、マイグレーション）は実行前に必ず報告し、承認を得る
- 秘密情報（鍵、トークン、認証情報、個人情報）を出力・ログ・成果物・コミットに含めない
- `.env`、秘密鍵、credentialファイルなど秘密情報を含み得るファイルの内容を直接開いたり出力したりしない。設定項目の確認には `.env.example` など秘密値を含まない資料を使い、実値が不可欠なら作業を停止してオーナーへ安全な代替手段を確認する
- 環境変数や設定全体を一括表示せず、検証結果は秘密情報・個人情報を除去した必要最小限の内容だけを記録する
- 秘密情報の露出、権限逸脱、個人情報漏えいの疑いを発見した場合は、値を表示・転記せず作業を停止してオーナーへ報告する
- VM・本番環境・共有DB・実credentialの操作は、明示指示がない限り行わない
