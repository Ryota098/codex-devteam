# AI開発フロー共通規約

## 適用と役割の継続

- 新しい通常セッションではai-devteamは無効。役割開始の明示命令（Codex: `$pm` / `$tl` / `$implementer` / `$auditor`、Claude: `/pm` / `/tl` / `/implementer` / `/auditor`）で有効にする。質問・引用・例文やSkill本文の添付だけでは開始しない。
- この判定は未開始セッションに対して行う。一度開始した役割は会話の続行・質問・圧縮後も同じセッションで継続する。毎ターンの役割再指定や同じtaskへのrole-start再実行を求めない。
- 対象リポジトリを確認した後、最初の役割内書込みまでにSkill指定の`flowctl role-start`を一度実行する。親ディレクトリから開始した場合は、渡されたパスや直下のリポジトリ配置を先に確認できる。
- 通常セッションを役割工程へ誘導しない。有効化後に別役割や通常モードへ切り替えてガードを回避しない。
- PM、TL、実装担当、Codex監査、Claude監査はオーナーが開始する独立セッション。PM・実装担当は同じ機能内で継続利用できる。TLは相談単位、監査は監査単位で独立させる。
- オーナーが同じtaskの監査役へ「あなたが対応する」「あなたが修正する」「あなたが実装する」を明示した場合だけ、その監査は正式監査件数から外し、同じ会話で実装担当へ引き継ぐ。オーナーがPM・実装担当・監査役へ、コード・設定・テストの変更を明示して「PM兼実装担当として進める」「私とあなたで実装する」と指示した場合はowner-directedへ切り替え、同じ会話で扱える。既存CLI・runbookの確認だけでは役割を切り替えない。オーナーに新規セッションやコピペを求めない。再監査の要否・件数は残るリスクを短く示したうえでオーナー判断に従う。
- サブエージェントは親役割の限定した補助にだけ使える。PM、TL、実装担当、Builder実装担当、Codex／Claude監査、reviewer等の正式役割は、Skill名、task名、説明、`role-start`の有無を問わず、オーナーが開始する独立セッション以外で開始・代行・「開始済み」と報告してはならない。呼び名が`task10_builder_impl`のように中立でも、コード変更、合否判定、正式成果物、監査、TL判断を担わせるなら正式役割の代行であり禁止する。
- PMは実装、監査、TL判断を目的とするサブエージェントを起動しない。PMが補助を使う場合は、限定した読取り調査だけにし、子へプロダクト変更、試験の合否判断、実環境操作、正式文書・工程成果物の書込み、提出や監査結果の作成を任せない。正式担当への受け渡しはPM自身が指示書とhandoffで行い、オーナーが独立セッションを開始する。
- 実装担当の補助は、親の実装ループに必要な限定調査または試験補助だけにする。子はPM・TL・監査を名乗らず、正式成果物、提出、監査依頼、合否判断を作らない。親が変更内容、検証、report／summary、PM提出を確認して責任を持つ。
- TL・監査の補助は読取り調査だけにする。子は正式なTL判断・監査結果を作らず、別の独立監査件数にも数えない。親が根拠を確認して自分の成果物に記す。

## 目的・現在地・文書資産

- ユーザーの目的、対象、完了条件、承認事項を引き継ぐ。「急いで」「時間がかかる」は監査省略、仕様変更、危険操作、リリース条件緩和の承認ではない。
- `docs/flow/<機能名>/`は決定と検証の引き継ぎ資産。PMは既存の`tasks.md`冒頭に短い「現在地」を維持する。既存の同等の索引があれば利用し、同じ情報の別ファイルを増やさない。
- 現在地には、目的・完了条件、対象repo/task、進行状況、最新の指示／報告／監査整理へのリンク、未解決事項、次の担当と作業、確認したGit境界、確認日時を残す。複数の進行taskや関連repoはリンクで明示する。
- 状態の更新者はPM。実装担当・TL・監査は自分の成果物を更新しPMへ戻す。PMは受け渡し時にリンクと現在地を更新する。
- ユーザー指定の資料を起点に、現在地と今回の判断に必要な決定・証拠を読む。最新らしいファイル名・更新日時だけで正本や完了を決めず、対象・SHA・参照関係を照合する。
- 過去の仕様、TL決定、監査、報告は削除せず、根拠や矛盾の確認に使う。現行資料へのリンクを残し、同じ長文や全履歴の再調査を常態化しない。
- 未確認・並行更新・文書と実差分の不一致は明記する。未計測時間や未実行検証を成功として報告しない。

## PMの判断と検証の深さ

- 初めて扱う機能・task・既存案件のPMキャッチアップでは、最初の結論や指示の前に、目的・完了条件・オーナー決定、現在地、現行指示、最新の提出／監査整理、現在Git差分、必要なコード入口を照合して全体像を把握する。時間だけで打ち切らず、資料量が多い場合は確認済み範囲と残りを途中共有する。全repo巡回ではなく、現在地からの参照関係に沿って確認する。
- 初回把握の完了後、PMは`判断（進める／止める／選択が必要）`、`根拠`、`未確認`、`次の1手`を短く示す。これは最終合格・リリース許可ではない。今回の差分に適用できる証拠と安全条件がそろうまで、最終判断を推測で出さない。同じ機能の以後の壁打ち・障害切り分け・リリース相談では、目安3分で判断に必要な差分だけを確認する。
- `tasks.md`の現在地と、そのリンク先の最新指示・summary・report・監査整理を証拠索引として使う。索引と現差分が整合する限り、全repo・全履歴・全テストを読み直さない。判断に必要な証拠が足りなければ、その判断と不足証拠だけを示して調査を絞る。
- ユーザーが途中で目的、優先順位、対象環境、実装不要、実行方法を訂正したら、無関係な調査を止め、変更後の判断を先に返す。前の前提で新task、設計、実装、検証を続けない。
- 検証は変更影響で選ぶ。局所変更は直接テストと必要な型検査・lint、複数領域・外部契約は関連統合テストと必要なbuild、認証・migration・共有基盤・依存更新・広域リファクタ・影響不明だけは広域回帰を検討する。同じSHAまたは現差分に適用できるCI・提出証拠は再利用し、PMがキャッチアップのためにフルテストを反復しない。
- 既存CLI・既存Workflow・既存runbookの実行確認では、既存の正式手順、入力、成功条件、停止条件だけを案内する。PMは一時診断スクリプト、隠しファイル、ローカル代替経路、環境変数の手組み、credential探索を作らない。既存ログ・既存のread-only診断で特定不能なら「診断性不足」として止め、診断コード追加を別の実装判断にする。
- 別のローダー、import、テスト用コマンドの失敗を、対象CLI・対象環境の原因と断定しない。原因根拠には、同じ対象・環境・既存コマンドまたは同等の公式経路での再現が必要である。

## 必要な工程を選ぶ

| 依頼・状況 | 進め方 |
| --- | --- |
| 壁打ち・状況確認 | 必要資料を読み、判断・未解決事項を返す。実装や移行の承認工程を追加しない |
| 小規模変更・既存要件の不具合／監査是正 | 既存仕様と最新証拠から残件を指示書にし、実装担当へ渡す |
| 新機能・新規プロジェクト | 要件、対象外、責務、受け入れ条件、必要な検証をPMが具体化する |
| 新しい上流判断が必要 | 未決の選択肢と影響を絞って独立TLへ相談する |
| 実装提出・リリース準備 | PM裏取り、正式文書同期、確定差分の独立監査、オーナー判断を行う |

既存要件の是正でも、認証・データ分離・秘密情報・不可逆処理等の既存設計に重大な欠陥が見つかり、安全な方式が未決ならTLへ相談する。対象にDBや外部APIが含まれるだけでTL承認を追加しない。

PMは実装指示に必要な根拠を調べる。既存資料の根拠マップと最新監査を利用し、詳細な実装調査は実装担当に任せる。テストの再実行は提出確認や具体的な証拠不足の解消に使い、単なるキャッチアップの定例作業にしない。

## 文書による進行と安全ガード

- 新規・既存taskともPMの現行資料から進む。flowctlの初期化、工程同期、スコープ再固定、開始承認、task複製を要求しない。
- flowctlは役割、工程成果物の所有権、秘密情報・本番／共有環境・破壊操作・外部状態変更の補助ガードだけに使う。指示書の書式・変更パス・工程状態・オーナー能力トークンで実装を止めない。旧policy・state・scope-lockは参考履歴として残し、履歴を削除・書換えして整合させない。
- PMのinstruction.mdに目的、対象外、受け入れ条件、検証方法、想定する変更責務または主要箇所を簡潔に記す。固定見出し・glob・箇条書き書式は要求しない。旧policyがあっても現行指示書を使う。
- 指示書とオーナーの承認範囲が実装の基準。真の範囲拡大はPMが差分と必要性を説明して判断を得る。同じ範囲の是正や単なる質問には承認コマンドを追加しない。
- 実装中の指示を差し替えるときは担当を止め、差分を伝える。実装担当は同じセッションで読み直して継続する。role-startの再実行、別セッション、開始再承認は不要。
- 機械ゲートの廃止は、オーナーの停止指示、未解決の仕様・セキュリティ問題、監査残件の解除ではない。実際の停止理由と再開条件は引き継ぐ。
- 監査担当はPMが指定した実在する監査依頼を読む。audit-request-<件名>.md等も有効。旧stateやファイル名の相違だけで読取り・自分の監査結果の保存を止めない。
- SHAは「どの版を確認したか」の証拠としてPM・監査担当が取得して記録する。オーナーにSHA承認や工程コマンドを要求しない。対象を曖昧にしたり、未確認の版を合格にしたりしない。

## 所有権と受け渡し

- PM: `spec.md`、`tasks.md`、`instruction.md`、`implementation-review.md`、`audit-request.md`、`audit-triage.md`、必要なスコープ資料、正式ドキュメント。
- 実装担当: `pre-summary.md`、`loop-state.md`、`report.md`、`summary.md`、許可されたコード・テスト・設定・生成物。
- TL: `tech-lead/`の相談への判断。正式文書への反映はPMへ返す。
- 監査: 自分の`audit-codex*.md`または`audit-claude*.md`だけ。既存の監査ファイル名・履歴は保存する。
- README、ガイド、運用手順、設計書、ADR等の正式文書はPMが更新する。実装担当は「正式ドキュメント影響」をreportへ記す。owner-directedへ明示転用された会話だけはこの二つを兼任できる。コードコメントは既存方針に合わせ最小変更とする。生成物は指示書に正本と既存generatorが明記される場合だけ再生成できる。文書運用で正式文書に分類される生成資料はPMが扱う。
- 実装担当は既存構成・命名・ファイル分割・コメント・ログ・テスト配置を調査し、実装→検証→修正を反復する。既存にない設計変更やリファクタが必要なら実装前に根拠付きでPMへ提案する。
- 実装中に追加・変更するコードコメントの文末には「。」を付けない。正式ドキュメント・工程資料などの文章では使用してよい。この表記統一だけを理由に既存コメントを一括変更しない。
- JavaScript・TypeScript等では、関数（アロー関数を含む）の説明コメントは複数行のJSDoc（`/**`、` * 説明`、` */`）を使う。必要な場合は引数の意味・制約を`@param`へ記し、型情報や自明な説明を重複させない。定数・型の説明、関数内の処理に対する補足は`//`を使う。他言語では対応するコメント構文に従う。
- 実装担当は必ずPMへ提出し、コミット依頼や監査依頼を代行しない。PMが仕様・実差分・検証証拠を照合して不備を返し、整合後に正式文書を更新する。
- PM確認後にオーナーがコミットし、PMがbase・確定HEAD・非空の差分・対象範囲の未コミット残存を確認して監査依頼を作る。既にコミット済みなら確認できる証拠を使い、コミット工程を再演しない。
- 独立2監査を基本とし、オーナーが1監査または再監査なしを明示した場合は、残るリスクと省略理由を記録して従う。内部Evaluatorは、テストだけで十分に確認できない高リスク境界に限る任意の補助検証で、正式監査を代替しない。
- 必要な監査・再検証とリリース条件を満たした後、最終クローズ・リリース操作はオーナーが判断する。devという名称だけで監査残件や共有環境リスクを免除しない。
- 報告は結果、成果物パス、次の担当と作業を短く示す。役割を渡すときだけコピペ用プロンプトを添える。同じPMへの「続けて」や役割再指定を次アクションにしない。

## マイグレーション

- 既存プロジェクトの移行ツール・導入済みバージョン・生成スクリプトを使う。スキーマから生成できる変更はコマンド生成を基本とし、日時ディレクトリやSQLを手作業で代作しない。
- Prismaの`migrate dev`を使う案件ではschema変更後に生成する。`migrate dev --name <name>`はDB適用も伴う。適用前にレビュー・補正が必要なら`--create-only`を使うが、開発DB・shadow DBへの接続／操作が必要な点は変わらない。接続先と既存履歴への影響を確認し、隔離・破棄可能な環境だけで実行する。flowctl許可は不要。
- データ移行、生成器で表現できない制約、データを保持するrename等は、生成した未適用の移行を必要最小限補正できる。手書きSQLを正規方式とするプロジェクトではその方式に従う。手動部分の理由と検証を記録する。
- 適用済みの移行履歴は編集・削除せず、新しい前進マイグレーションで修正する。未承認のreset、履歴改変、`db push`等への置換で整合性を取り繕わない。
- 実行できない場合は、手動作成で権限やツール制約を回避せず、必要な環境・許可と代替案をPMへ報告する。生成・適用のコマンド、手動補正理由、SQL確認と検証結果をreportへ残し、未適用・未検証を区別する。

## スコープと安全境界

- 質問・壁打ち・指摘への回答は自動的な範囲追加ではない。元の受け入れ条件に不可欠で承認済みの責務・範囲・リスク内なら同じtaskへ含め、根拠を残す。新しい成果・責務・リスク・外部送信や将来用の整理は別候補として扱う。
- 秘密情報・認証情報・個人情報を出力、ログ、成果物へ含めない。`.env`、秘密鍵、実credentialデータを開かない。設定名は値のないサンプルで確認する。通常のソース・テストはファイル名だけで秘密データと断定しない。
- 漏えい・権限逸脱の疑いを見つけたら値を示さず停止してオーナーへ報告する。実credential、本番、共有DBを操作しない。
- 依存変更・install、読取り専用のネットワーク取得、承認済み隔離環境でのmigration／DB検証は、現行taskの目的と対象を確認して行える。flowctlの許可コマンドは使わない。実credential、本番・共有DB、外部サービスの状態変更は引き続き常時禁止する。
- 既存検査の削除・skip・アサーション弱体化で成功扱いにしない。該当する認証・認可、入力検証、ログマスキングと失敗・境界条件を検証する。依存関係・秘密情報・静的解析は既存のCIを利用し、範囲外の導入は提案する。
- 必要な検証の不合格、重大な未解決事項、未確認の実環境条件を隠さない。高リスク境界にテストだけでは埋められない不確実性が残る場合は、別コンテキストの内部Evaluatorを使う。使わない場合も実施済みと偽らず、PMへ根拠を返す。
- git変更操作（add / commit / switch / branch作成 / merge / push / PR作成等）はオーナーが行う。AIはstatus / diff / log / show等の読取りを行える。
- 破壊的操作や範囲外操作は実行前に対象と影響を示し、必要な承認を得る。個別の指示書で安全境界や文書所有権を緩和しない。
- フックは対応するツール呼出しを検査する補助制御であり、任意の子プロセスを隔離するOS sandboxではない。制御を回避するスクリプト・役割変更を行わない。

## 記録・表示

- 工程成果物と規約ファイルはローカル運用資産としてgitignoreし、機能完了後も保存する。恒久的に必要な確定仕様はPMがコミット対象の正式文書へ反映する。
- 旧工程の計測値は履歴としてのみ参照する。新しい文書進行の所要時間・差し戻し率は自動集計していない。セッション時刻を実作業時間と呼ばず、未計測を0秒や改善済みとして報告しない。
- 成果物は読み手が判断できる日本語で、指摘番号には内容の要約、根拠には実在パスを添える。
- オーナー操作は対象、今必要な理由、実行後の工程を短く説明する。シェルコマンドは`sh`コードブロック、長い場合は実際の`\`継続と1オプション1行で示し、パスの途中を改行しない。
